# CNN-from-scratch-numpy-
This CNN model is coded in python while only using the numpy library. It includes a Convolution layer, a Max Pooling layer and a fully Connected layer class. Additionaly I have implemented the Adam-optimiser and a neuron-dropout
